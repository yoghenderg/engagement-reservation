const $=s=>document.querySelector(s);let settings,requestId=crypto.randomUUID(),busy=false;const form=$('#rsvp');
const SITE_CONFIG={closesAt:'2026-10-12T00:00:00+08:00',background:'/assets/couple-maroon.png',backgroundPosition:'center bottom',supabaseUrl:'https://bdooschvskhsyxripctz.supabase.co',supabaseAnonKey:'sb_publishable_56ryK_rIZjtDb1cKufD_-w_flEdM0yl'};
function closed(c){return new Date()>=new Date(c.closesAt);}
async function getConfig(){settings={...SITE_CONFIG,closed:closed(SITE_CONFIG)};const deadline=new Date(new Date(settings.closesAt).getTime()-1).toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric',timeZone:'Asia/Kuala_Lumpur'});$('#deadline').textContent=deadline;document.querySelectorAll('.deadline-copy').forEach(e=>e.textContent=deadline);$('.backdrop').style.setProperty('--mobile-photo',`url(${JSON.stringify(settings.background)})`);$('.backdrop').style.backgroundPosition=settings.backgroundPosition||'center';return settings;}
function show(page,push=true){for(const id of ['landing','reservation','thanks'])$('#'+id).hidden=id!==page;if(push)history.pushState({page},'',page==='landing'?'/':page==='reservation'?'/reserve':'/thank-you');window.scrollTo({top:0,behavior:'instant'});if(page!=='landing')setTimeout(()=>$('#'+(page==='reservation'?'form-title':'thanks-title')).focus({preventScroll:true}),50);}
function closeNotice(){$('#closed-dialog').showModal();}
async function reserve(push=true){const b=$('#reserve');b.disabled=true;try{const c=await getConfig();if(c.closed)closeNotice();else show('reservation',push);}catch(e){b.textContent='Connection unavailable · Try again';}finally{b.disabled=false;}}
$('#reserve').onclick=()=>reserve();$('#back').onclick=()=>show('landing');$('#home').onclick=()=>{form.reset();update();requestId=crypto.randomUUID();show('landing');};$('.brand').onclick=e=>{e.preventDefault();show('landing');};$('.close').onclick=()=>$('#closed-dialog').close();
function update(){const yes=form.elements.attending.value==='yes';$('#attending-fields').hidden=!yes;for(const el of [$('#guests'),$('#meal')]){el.required=yes;el.disabled=!yes;}$('.submit').disabled=busy||!form.checkValidity()||$('#name').value.trim().length<2||$('#phone').value.replace(/\D/g,'').length<7;$('.submit').firstChild.textContent=form.elements.attending.value==='no'?'Send your reply ':'Confirm reservation ';}
form.addEventListener('input',update);form.addEventListener('change',update);
async function saveReservation(data){if(closed(settings))return {closed:true};if(!settings.supabaseUrl||!settings.supabaseAnonKey)throw Error('Reservation database is not connected yet.');const payload={request_id:requestId,full_name:data.name.trim(),phone:data.phone.trim(),attending:data.attending,guests:data.attending==='yes'?data.guests:null,meal:data.attending==='yes'?data.meal:null};const r=await fetch(`${settings.supabaseUrl}/rest/v1/reservations`,{method:'POST',headers:{apikey:settings.supabaseAnonKey,Authorization:`Bearer ${settings.supabaseAnonKey}`,'Content-Type':'application/json',Prefer:'resolution=ignore-duplicates,return=minimal'},body:JSON.stringify(payload)});if(r.status===409)return {ok:true};if(!r.ok)throw Error('We couldn’t save your reply. Please try again.');return {ok:true};}
form.onsubmit=async e=>{e.preventDefault();if(busy||!form.reportValidity())return;busy=true;update();$('.submit').firstChild.textContent='Sending your reply… ';$('#error').textContent='';const data=Object.fromEntries(new FormData(form));try{const result=await saveReservation(data);if(result.closed){closeNotice();return;}const yes=data.attending==='yes';$('#thanks-title').innerHTML=yes?'You’re on<br><em>our guest list.</em>':'You’ll be<br><em>with us in spirit.</em>';$('#thanks-copy').innerHTML=yes?'We’ll be waiting for your arrival.<br>Thank you for being part of our beginning.':'Thank you for letting us know.<br>We’ll miss you and keep you close in our hearts.';$('#receipt').textContent=yes?`${data.guests} ${data.guests==='1'?'guest':'guests'} · ${data.meal==='vegetarian'?'Vegetarian':'Non-vegetarian'}`:'Your reply has been received.';show('thanks');}catch(e){$('#error').textContent=e.message||'We couldn’t save your reply. Please try again.';}finally{busy=false;update();}};
window.onpopstate=()=>{if(location.pathname==='/reserve')reserve(false);else show('landing',false);};update();getConfig().then(()=>{if(location.pathname==='/reserve'){if(settings.closed){show('landing',false);closeNotice();}else show('reservation',false);}else show('landing',false);}).catch(()=>{});
